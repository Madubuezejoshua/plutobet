# Inngest exposes optional integrations for SvelteKit as peer dependencies.
# npm otherwise tries to resolve that unused Vite 8 peer against Vitest's
# Vite 7 graph. The application uses Inngest's Next.js adapter only.
legacy-peer-deps=true
